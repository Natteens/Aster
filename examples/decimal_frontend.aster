// Decimal syntax and typed frontend/IR representation exist, but the current
// public CLI validates backend support before success. `aster check`,
// `aster dump-hir`, `aster dump-mir`, and `aster run` reject this fixture until
// the decimal runtime representation and ABI are implemented.

public decimal Price()
{
    decimal value = 19.95m;
    return value;
}
